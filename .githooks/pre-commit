#!/usr/bin/env bash
# Direct-call fallback for the canonical quality gate. Commit and push hooks are
# managed by pre-commit from .pre-commit-config.yaml; do not set core.hooksPath
# to .githooks.
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
exec "${repo_root}/scripts/quality-gate.sh"
